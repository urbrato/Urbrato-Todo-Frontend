import { PrioritySearchDto } from './priority-search-dto';

describe('PrioritySearchDto', () => {
  it('should create an instance', () => {
    expect(new PrioritySearchDto()).toBeTruthy();
  });
});
