import { CategoryCreateDto } from './category-create-dto';

describe('CategoryCreateDto', () => {
  it('should create an instance', () => {
    expect(new CategoryCreateDto()).toBeTruthy();
  });
});
