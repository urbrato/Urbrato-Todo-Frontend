import { PriorityUpdateDto } from './priority-update-dto';

describe('PriorityUpdateDto', () => {
  it('should create an instance', () => {
    expect(new PriorityUpdateDto()).toBeTruthy();
  });
});
