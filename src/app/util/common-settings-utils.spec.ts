import { CommonSettingsUtils } from './common-settings-utils';

describe('CommonSettingsUtils', () => {
  it('should create an instance', () => {
    expect(new CommonSettingsUtils()).toBeTruthy();
  });
});
