import { LocalStorageUtils } from './local-storage-utils';

describe('LocalStorageUtils', () => {
  it('should create an instance', () => {
    expect(new LocalStorageUtils()).toBeTruthy();
  });
});
